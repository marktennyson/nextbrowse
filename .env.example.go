# Go Backend Environment Configuration

# Root directory on your local machine to browse (REQUIRED)
ROOT_PATH=/path/to/your/local/directory

# Port for the Go backend server (optional, defaults to 8080)
GO_PORT=8080

# Base URL for Next.js frontend (for share URLs)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Go API URL for frontend to call (optional, defaults to http://localhost:8080)
NEXT_PUBLIC_GO_API_URL=http://localhost:8080

# Port for Next.js frontend (optional, defaults to 3000)
NEXT_PORT=3000